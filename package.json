{
  "name": "exampleRs",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "yarn plugin-helpers build",
    "build-wasm": "wasm-pack build --target nodejs --out-dir ../../server/wasm wasm/kibana_plugin --features server && wasm-pack build --out-dir ../../public/wasm wasm/kibana_plugin --features public",
    "plugin-helpers": "node ../../scripts/plugin_helpers",
    "kbn": "node ../../scripts/kbn"
  }
}
